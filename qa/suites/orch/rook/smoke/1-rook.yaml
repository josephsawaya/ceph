tasks:
- rook:
- rook.shell:
    - ceph -s
    - ceph orch status
    - ceph orch ps
    - ceph orch ls
    - ceph orch device ls
    - ceph orch apply osd --all-available-devices
    - ceph orch apply rgw rgw-test
    - ceph orch apply nfs nfs-test
    - ceph orch osd rm 0 --replace